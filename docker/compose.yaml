version: '3.8'

services:
  tectonic:
    build:
      context: .
      dockerfile: docker/Dockerfile.tectonic
    volumes:
      - ./temp:/work
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp
    user: "1001:1001"
